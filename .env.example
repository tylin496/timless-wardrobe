# Supabase project (Dashboard → Settings → API)
SUPABASE_URL=https://yyzrzmbsxphlhoqzikjn.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Server-side only: bulk import script (scripts/import_seed_to_supabase.mjs).
# Never expose in the browser or commit to git.
SUPABASE_SERVICE_ROLE_KEY=

# Optional: Vite / bundler builds can expose public keys with this prefix instead
# of using js/tw-supabase-config.js — not used by the vanilla static entry today.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
